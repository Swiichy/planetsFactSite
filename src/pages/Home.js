import React from 'react';
import Navigation from '../components/Navigation';

const Home = () => {
    return (
        <div className="container">
            <Navigation />
        </div>
    );
};

export default Home;